Hallo <%= @volunteer.name %>,

Iemand (waarschijnlijk uzelf) heeft u als vrijwillige aangeduid om te helpen op "<%= Settings.event_name %>" (<%= Settings.event_name %>) voor de volgende taken:

<% @volunteer.applications.each do |application| %>
    - <%= application.name %> van <%= application.hour %> tot <%= "%02d" % (application.hour.to_i + 1) %>
<% end %>

U kan het volledige uurrooster zien op <%= schedule_url %>.

Indien u niet betrokken bent, of indien deze inschrijving niet correct is, gelieve dan zo snel mogelijk <%= Settings.event_owner %> te contacteren.

Ook voor eventuele wijzigingen aan uw inschrijving kan u <%= Settings.event_owner %> contacteren.

Met hartelijk dank voor uw hulp,
het Beschermcomité