<%= form_for(@timeslot) do |f| %>
  <% if @timeslot.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <strong><%= pluralize(@timeslot.errors.count, "error") %> prohibited this timeslot from being saved:</strong>

      <ul>
      <% @timeslot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br />
    <%= f.text_field :name, :class => "form-control col-sm-4" %>
  </div>
  <div class="form-group">
    <%= f.label :sorting %><br />
    <%= f.number_field :sorting, :class => "form-control col-sm-2" %>
  </div>
  <div class="form-group">
    <%= f.submit 'Save' , :class => "btn btn-primary" %>
  </div>
<% end %>
